import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('smoke test placeholder', (WidgetTester tester) async {
    // Real UI tests require Riverpod + DB setup — placeholder until test harness is built.
    expect(true, isTrue);
  });
}
